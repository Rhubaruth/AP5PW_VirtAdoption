@using VanaKrizan.Utulek.Infrastructure.HelperClasses;
@using VanaKrizan.Utulek.Web.Areas.admin.Controllers;
@model UserPets

@{
    ViewData["Title"] = ViewBag.Username + "'s " + " pets";
}

<div class="titulEdit">
    <h2>@ViewData["Title"]</h2>
</div>


<div class="down">
    <form asp-action="UsersPets" id="form" method="post" class="petEdit">
        <div class="mb-3 inputy">

            <label class="form-label" asp-for="addPetId"> Přidat mazlíčka: </label>
            <div class="sel">
                <select class="sel-control" asp-for="addPetId" asp-items="@(new SelectList(Model.otherPets, "Id", "Name"))"
                        onfocus="this.size=@Math.Min(Model.otherPets.Count, 3);" onblur="this.size=0" onchange="this.size=1; this.blur()"></select>
            </div>

            <label class="form-label" asp-for="removePetId"> Odebrat mazlíčka: </label>
            <div class="sel">
                <select class="sel-control" asp-for="removePetId" asp-items="@(new SelectList(Model.adoptedPets, "Id", "Name"))"
                        onfocus="this.size=@Math.Min(Model.adoptedPets.Count, 3);" onblur="this.size=0" onchange="this.size=1; this.blur()"></select>
            </div>
        </div>

            <div class="btnSub">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>

            @* Hidden data from form *@
            <input type="hidden" asp-for="UserId" />


    </form>
</div>




@section Styles {
    <link rel="stylesheet" href="~/css/admin.css" asp-append-version="true" />

}
