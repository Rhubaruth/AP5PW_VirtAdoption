@using VanaKrizan.Utulek.Domain.Entities;
@model Pet

@{
    <div class="containerMazl">
        <section class="info">
            <div class="row">
                <div class="mazel">
                    <h1>@Model.Name</h1>
                    <div class="zaklInfo">
                        <dl class="zaklad">
                            <fieldset>
                                <legend>Základní údaje</legend>
                                <dl>
                                    <dt class="hl">Velikost:</dt>
                                    <dd class="data">[not in db yet]</dd>
                                </dl>
                                <dl>
                                    <dt class="hl">Plemeno:</dt>
                                    <dd class="data">[not in db yet]</dd>
                                </dl>
                                <dl>
                                    <dt class="hl">Stáří:</dt>
                                    <dd class="data">@Model.Birth</dd>
                                </dl>
                                <dl>
                                    <dt class="hl">Pohlaví:</dt>
                                    <dd class="data">@Model.Sex</dd>
                                </dl>
                                <dl>
                                    <dt class="hl">V útulku od:</dt>
                                    <dd class="data">@Model.InShelterSince</dd>
                                </dl>
                                <dl>
                                    <dt class="hl">Čip:</dt>
                                    <dd class="data">[not in db yet]</dd>
                                </dl>
                            </fieldset>
                        </dl>
                        <div class="obr">
                            <img src=@Model.ImageSrc>
                        </div>
                    </div>
                    <div class="popis">
                        @Model.Info
                    </div>
                    
                </div>
            </div>
        </section>
    </div>
}

@section Styles {
    <link rel="stylesheet" href="~/css/detail.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
}
